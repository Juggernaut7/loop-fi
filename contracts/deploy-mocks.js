const hre = require("hardhat");async function main() {console.log("Deploying mocks...");const MockERC20 = await hre.ethers.getContractFactory("MockERC20");const mockCelo = await MockERC20.deploy("Mock CELO", "mCELO");await mockCelo.deployed();console.log("Mock CELO:", mockCelo.address);const mockCUSD = await MockERC20.deploy("Mock cUSD", "mcUSD");await mockCUSD.deployed();console.log("Mock cUSD:", mockCUSD.address);const [deployer] = await hre.ethers.getSigners();const mintAmount = hre.ethers.utils.parseEther("1000000");await mockCelo.mint(deployer.address, mintAmount);await mockCUSD.mint(deployer.address, mintAmount);console.log("Minted 1M tokens to:", deployer.address);}main().then(() => process.exit(0)).catch((error) => {console.error(error);process.exit(1);});
